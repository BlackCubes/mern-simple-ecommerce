const productRouter = require('./productRoutes');
const reviewRouter = require('./reviewRoutes');
const userRouter = require('./userRoutes');

module.exports = { productRouter, reviewRouter, userRouter };
